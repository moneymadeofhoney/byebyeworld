В книге n страниц. Найти количество цифр, необходимое для нумерации всех страниц такой книге. Решить обратную задачу: зная количество понадобившихся для нумерации цифр, определить количество страниц в книге.

def num_digits(n):
  """
  Функция для вычисления количества цифр, необходимых для нумерации страниц книги.

  Args:
    n: Количество страниц в книге.

  Returns:
    Количество цифр.
  """

  if n <= 9:
    return 1
  elif n <= 99:
    return 2
  elif n <= 999:
    return 3
  else:
    # Количество цифр в числе n
    digits = int(math.floor(math.log10(n)) + 1)
    # Количество страниц с 1-значными номерами
    single_digit_pages = 9
    # Количество страниц с 2-значными номерами
    double_digit_pages = 90
    # Количество страниц с 3-значными номерами
    triple_digit_pages = n - single_digit_pages - double_digit_pages
    # Общее количество цифр
    return digits * (triple_digit_pages + double_digit_pages // 10 + single_digit_pages // 100)


# Ввод n
n = int(input("Введите количество страниц: "))

# Вычисление количества цифр
num_digits_n = num_digits(n)

# Вывод результата
print(f"Для нумерации {n} страниц требуется {num_digits_n} цифр.")
